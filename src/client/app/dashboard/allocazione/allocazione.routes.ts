import { Route } from '@angular/router';

import { AllocazioneComponent } from './index';

export const AllocazioneRoutes: Route[] = [
	{
		path: 'allocazione',
		component: AllocazioneComponent
	},
];
